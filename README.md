# GC/LC Recalc Area% Calculator (Streamlit)

Paste an Excel table (TSV) and the app calculates:
- Area sum
- Air peak / Si peak / No data sums
- Recalc sum
- Recalc % for relevant peaks + validation

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
